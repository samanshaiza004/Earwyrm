{
  "name": "bun-api",
  "version": "1.0.50",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
