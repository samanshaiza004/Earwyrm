{
  "name": "fullstack-for-bun-api",
  "version": "1.0.50",
  "workspaces": [
    "packages/*"
  ],
  "trustedDependencies": [
    "@prisma/client",
    "@prisma/engines",
    "prisma"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "frontend": "bun --filter frontend",
    "backend": "bun --filter bun-api"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
